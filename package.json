{
  "name": "inversify-rest",
  "version": "1.0.0",
  "description": "",
  "main": "dist/app.js",
  "dependencies": {
    "@types/express": "^4.0.36",
    "@types/node": "^8.0.7",
    "express": "^4.15.3",
    "inversify": "^4.1.1",
    "reflect-metadata": "^0.1.10",
    "ts-node": "^3.1.0",
    "typescript": "^2.4.1"
  },
  "devDependencies": {
    "tslint": "^5.4.3"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "tsc && cp -R data dist",
    "start": "node dist/app.js",
    "serve": "tsc && cp -R data dist && node dist/app.js",
    "lint": "tslint --project tsconfig.json --type-check --fix"
  },
  "author": "",
  "license": "ISC"
}
